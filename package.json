{
  "name": "cssomnom",
  "version": "0.1.0",
  "description": "Fast, spec-compliant CSS Object Model (CSSOM) and Typed OM parser in pure TypeScript",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/GoogleChromeLabs/cssomnom.git"
  },
  "bugs": {
    "url": "https://github.com/GoogleChromeLabs/cssomnom/issues"
  },
  "homepage": "https://github.com/GoogleChromeLabs/cssomnom#readme",
  "keywords": [
    "css",
    "cssom",
    "typed-om",
    "parser",
    "ast",
    "stylesheet",
    "w3c"
  ],
  "type": "module",
  "sideEffects": false,
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "API_BOUNDARIES.md",
    "pnpm-lock.yaml",
    "pnpm-workspace.yaml"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "engines": {
    "node": ">=24.11.0"
  },
  "packageManager": "pnpm@11.10.0",
  "scripts": {
    "build": "tsup && tsup --config tsup.browser.config.ts && tsc -p tsconfig.build.json",
    "prepublishOnly": "pnpm run preflight && pnpm run build",
    "test": "pnpm run preflight",
    "test:node": "node --test --test-reporter=dot 'tests/**/*.test.ts'",
    "test:wpt-typed-om:chrome": "pnpm run build && (python3 submodules/web-platform-tests/wpt run --processes 16 --headless -y --log-wptreport dist/report-chrome.json --log-wptscreenshot dist/chrome-screenshots.txt --log-html dist/report-chrome.html --inject-script dist/cssomnom.iife.global.js chrome css/css-typed-om || true) && node scripts/generate_wpt_report.ts",
    "test:wpt-typed-om:firefox": "pnpm run build && (python3 submodules/web-platform-tests/wpt run --processes 16 --headless -y --log-wptreport dist/report-firefox.json --log-wptscreenshot dist/firefox-screenshots.txt --log-html dist/report-firefox.html --inject-script dist/cssomnom.iife.global.js firefox css/css-typed-om || true) && node scripts/generate_wpt_report.ts",
    "typecheck": "tsc --noEmit",
    "preflight": "pnpm typecheck && pnpm lint && pnpm test:node",
    "lint": "oxlint",
    "fixtures:generate": "node scripts/extract_external_suites.ts",
    "external:extract": "node scripts/extract_external_suites.ts",
    "baselines:prune": "node scripts/prune_resolved_failures.ts",
    "wpt:sandbox": "node scripts/run_wpt_sandbox.ts",
    "wpt:baseline": "node scripts/run_wpt_crawler.ts --update-baseline",
    "wpt:progress": "node scripts/run_wpt_crawler.ts --update-progress",
    "submodules:update": "git submodule update --init --recursive",
    "submodules:upgrade": "git submodule update --init --remote && pnpm run submodules:update",
    "codegen": "node scripts/generate_all.ts",
    "maintain": "pnpm update --latest && pnpm run submodules:upgrade && pnpm run codegen && pnpm run external:extract && pnpm run preflight"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@webref/css": "^8.7.0",
    "linkedom": "^0.18.13",
    "mdn-data": "^2.28.1",
    "oxlint": "^1.74.0",
    "tsup": "^8.5.1",
    "typescript": "^7.0.2"
  }
}